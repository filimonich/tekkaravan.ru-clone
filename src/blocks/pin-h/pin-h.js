(() => {

	// const $pin = $('.pin-h');
	// const $toggle = $pin.find('.pin-h__toggle');
	
	// открыть фильтр
	/* const open = (e) => {
		e.preventDefault();
		e.stopPropagation();

		if(!$pin.hasClass('opened'))
			$pin.addClass('opened');
	} */
	
	// закрыть фильтр
	/* const close = (e) => {
		e.preventDefault();
		e.stopPropagation();
		
		if($pin.hasClass('opened')) 
			$pin.removeClass('opened');
	} */
	
	/* $toggle.on('click', (e) => $pin.hasClass('opened') ? close(e) : open(e));

	document.addEventListener('click', (e) => {
		const isopened = $pin.hasClass('opened');
		const isnavi = e.target.closest('.pin-h__block');

		if(isopened && !isnavi)
			close(e);
	});
		
	$pin[0].addEventListener('swiped-up', (e) =>  close(e)); */

})();

